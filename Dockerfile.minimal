FROM node:18-alpine

WORKDIR /app

# Install minimal dependencies
COPY package.json ./
RUN npm install express

# Copy our simple server only
COPY src/simple-server.js ./

# Expose port 8080
EXPOSE 8080

# Start the minimal server
CMD ["node", "simple-server.js"]
