# Database Connection
# Local development connection
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/task_manager_db"

# Cloud SQL connection via Unix socket (recommended for Cloud Run)
# Replace PROJECT_ID, REGION, and INSTANCE with your Cloud SQL details
DATABASE_URL="postgresql://postgres:postgres@/task_manager_db?host=/cloudsql/PROJECT_ID:REGION:INSTANCE"

# Cloud SQL connection via TCP with credential params
# DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:5432/task_manager_db"

# PostgreSQL configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=task_manager_db
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Cloud SQL configuration
INSTANCE_CONNECTION_NAME="PROJECT_ID:REGION:INSTANCE"

# Application Settings
PORT=8080
NODE_ENV=production
# For local development: "http://localhost:8080"
# For Cloud Run: "https://campus-exchange-xxxxx.a.run.app"
BASE_URL="https://campus-exchange-xxxxx.a.run.app"

# JWT Authentication
JWT_SECRET="your-secret-key-here"
JWT_EXPIRES_IN="7d"

# Email Configuration
EMAIL_HOST="smtp.gmail.com"
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=demoprojectv07@gmail.com
EMAIL_PASS=ryhbisthrkemixpf
EMAIL_FROM=demoprojectv07@gmail.com