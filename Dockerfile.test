FROM node:18-alpine

WORKDIR /app

# Copy only what we need for the test server
COPY test-package.json ./package.json
COPY test-server.js ./

# Install only express
RUN npm install

# Expose the port
EXPOSE 8080

# Start the server
CMD ["node", "test-server.js"]
